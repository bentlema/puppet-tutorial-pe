#!/bin/bash

curl -O https://download.docker.com/mac/stable/Docker.dmg
curl -O https://download.docker.com/win/stable/InstallDocker.msi

# Docker on Linux is installed via packages, so need to write
# a script to do it.  Will come back to this later.
# https://docs.docker.com/engine/installation/linux/

